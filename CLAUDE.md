- 使用中文交流
- 使用uv管理依赖和执行py脚本
- 每次添加依赖都要使用uv add保证toml文件是最新的
- 不需要生成任何说明文档
- 项目内所有开发流程必须走 CodeStable 技能：新功能走 `cs-feat`，修 bug 走 `cs-issue`，重构走 `cs-refactor`，需求更新走 `cs-req`，审计走 `cs-audit`，知识沉淀走 `cs-keep` / `cs-note`；不要绕过对应 `cs-*` 流程直接实施。

