---
type: refactor
status: complete
updated: 2026-09-08
---

# AUTOBN I/O 与变量复用优化

## 目标

按照用户提供的代码简洁与执行效率标准，落实最新审查建议：本地过滤前置、平仓记录先入队并在扫描收尾刷盘、复用本轮 BD 取数结果、减少持仓解析和交易对遍历、首次止盈只赋最终目标，清理同义别名。

## 现场

- 基线为 `dev@988bd45f` 上尚未提交的已完成持仓流程重构，承接 `refactor-autobn-position-flow.md`。
- 开始时 `autoTrade_pm.py` SHA256：`C05E3FF4ED1C2E33A5EE5086C2033ACF61AD416028B7F092E3881AB67055327E`。
- 已模拟观察：无效止损参数仍发出 2 次请求；成交量已淘汰的 BD 仍取 OI；一次无 OI 的 BD 扫描取数 3 次；一条已存在仓位被解析 2 次。
- 共享工作区另有 AUTOA 优化任务，保留其实现、测试、文档、游标以及业务 Excel 修改。

## 边界

- 开仓条件、数量、方向、同币单仓、多头仅 OI 止损、空头止损、止盈/DCA 优先级保持。
- 用户暂缓的超时订单重复止盈、同币双向仓位不处理；不改变价格/ATR/切比雪夫口径。
- 平仓状态与交易记录先完成内存写入，余仓核对和通知不等待磁盘；正常、异常、取消与退出路径仍有刷盘保障。
- 本轮取数失败也视为已取数；下一轮允许重试，不新增跨轮缓存或共享可变状态，不跨交易复用失效值。
- 保留零 ATR 与无效 ATR 结果分支；同语义变量复用，不强行压缩表达式。
- 不调用真实交易、行情和通知，不覆盖业务 JSON/Excel，不自行提交或推送。

## 证据

- 新增 `test_autobn_efficiency.py` 8 项测试，修改前复现 16 个失败子场景及 1 个精度字段异常，修改后全部通过。
- 平仓使用既有 `enqueue_close`，`rzq_market` 的 finally 统一刷盘；异常、超时、取消及写盘失败下一轮重试均已验证。
- 无效开仓参数的账户/行情请求 2→0；同轮 BD OI 取数失败 3→1，成功 2→1；仓位解析 2→1；交易对遍历 2→1。
- 完整现有模块 + 持仓流程回归 + 本轮效率回归共 187 项通过。测试使用临时 Excel 路径或模拟写入，不影响业务文件。
- 对本轮开始时的 AUTOBN 做输出对照：432 组首盈/普通/无止盈阶段及零/负/有效 ATR，24 组持仓同步，160 组 BD 判断，16 组交易对筛选及精度，全部一致。
- 外部调用 `Task.cancel()` 的定向验证通过：已入队记录刷盘一次，取消信号继续传播。
- Ruff、`git diff --check` 与独立审查通过，无 blocking/important。审查基于本轮开始时的源类快照，临时快照在交付前移除。
- 冻结 AST：AUTOBN/CloseExecution `8e7673a5c8dc414c435c36c1d91e94f88af73134b8e20e1f78d0bb8c394ea3d0`；指定现有四个测试类 `8477e038aa48e2bebec23e2d10c562d50f8a58f55ab28c86a9a752d5f6366e53`；持仓回归 `c20361b13c1cc286b3756606c0d6ef6b66e0e58049a64edee74ff71ff429fd00`；效率回归 `7cdfd0477b4dfe5fcf07fa976ad4f56266324b1a0682743936b5bf3ab16d03b3`。
- 验证：`uv run --no-sync python -B -m unittest tokenDemo.test_autobn_efficiency`；完整 187 项通过 `uv run --no-sync python -B -` 加载 `test_autoTrade_pm`、`test_autobn_position_flow`、`test_autobn_efficiency`，将 Excel 写入目标设为临时目录；Ruff 覆盖上述代码和测试。

## 验收

- [x] 本地开仓参数失败时不请求账户/行情，有效下单行为一致。
- [x] 平仓记录同步入队，扫描收尾正常/异常/取消均刷盘，记录与仓位/通知内容正确。
- [x] BD 本地价格/成交量先过滤，同轮成功或失败的 OI 结果复用，下轮重新获取。
- [x] 持仓单次解析并保持展示和仓位同步语义；symbol 成员过滤使用集合。
- [x] 交易对元信息一次遍历，先筛选再计算精度。
- [x] 首次/普通止盈最终目标与原结果一致；同义别名与重复收益距离计算清理。
- [x] 回归、静态检查和独立复核通过，work 更新为完成。

## 状态与未决

已完成：187 项回归、632 组新旧输出对照、Ruff、差异检查和独立复核通过。用户已授权本次与持仓流程优化一并提交推送。
毕业去向：稳定约束由 AUTOBN 实现及 `test_autobn_efficiency.py`、原持仓回归测试承载；无额外 lesson 或说明文档。
按用户要求保留本 work 游标；其他任务文件和业务数据修改保持。
